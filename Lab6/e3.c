#include<stdio.h>

float circleArea(float radius)
float circleperi(float radius)
float recArea(float height, float )
int main(voide)
{
	float radius,height,CArea,Cperi,RArea,Area;
	
	printf("Enter radius :");
	scanf("%.2f", &radius);
	
	printf("Enter height :");
	scanf("%.2f", &height);
	
	CArea = circleArea(radius);
	Cperi = circleperi(radius);
	RArea = recArea(height);
		
}